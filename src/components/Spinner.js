import loading_spinner from '../waiting.gif'

const Spinner = () => {
    return (
        <div>
            <img src={loading_spinner} alt="loading..." />
        </div>
    )
}

export default Spinner
